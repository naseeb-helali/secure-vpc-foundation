region        = "us-east-1"
project       = "secure-vpc-foundation"
instance_type = "t3.micro"

# Replace with your IP before apply (recommended: /32)
admin_cidr    = "0.0.0.0/0"

# Optional: existing EC2 key pair name for SSH
key_name      = ""
